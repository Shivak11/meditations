# Public visual Meditations

Update the public meditations skill with the local visual-study behaviour and literal-language correction. Bundle five distinct fallback design systems that need no StyleRef, Impeccable, remote fonts, extra packages or private user setup. Choose randomly on each new study, avoid recent choices when history is available, preserve a selected system during revisions and keep the explanation related to the subject.

The public skill name remains meditations. Preserve Claude Chat and coding-agent portability, actual-evidence reflection, the user's corrections/assumptions, counterexamples, next experiments, and explicit approval for lasting notes. Creating the requested private HTML is ordinary output and must not be stopped by the NO RECORD state. Prefer text if explicitly requested or file creation is unavailable. Never ship private session material, local hooks, account configuration or unpublished workflow-discovery instructions.

## Ownership

The orchestrator owns SKILL.md, existing reflection references, the new visual-study-output.md, README, skills.sh.json, package allowlist, package tests, evaluation metadata, root ZIP, integration and release. A design worker owns only skills/meditations/assets/design-systems/*, skills/meditations/references/fallback-designs.md, skills/meditations/scripts/select_design.py and tests/test_select_design.py. Evaluation agents own only their assigned outputs outside the public repo; a fresh reviewer is read-only. Preserve foreign edits; snapshot and stop on a collision.

## Verification

Use stdlib selection with seeded reproducibility for tests and system randomness by default. Validate exclusions, missing history, all-excluded recovery, offline assets, contrast and essential type sizes. Test the package allowlist, relative-resource closure and ZIP byte parity. Run a paired old/new synthetic reflection without design MCPs and boundary scenarios for text-only/no-file and private inputs. Inspect generated HTML on desktop and mobile, keyboard/reveal behaviour and print. Keep private evaluation outputs out of the public archive.

Rebuild meditations.zip in the repository root and retain the beginner upload route before npx. Commit verified work narrowly. Update the public repository after checking the remote base and publication contents; read the remote commit and downloaded ZIP back. If network access prevents publication, retain the ready-to-upload local ZIP and report the precise unproved state.
